import ReactDOM from 'react-dom/client';
import '@/shared/styles/index.css';
import React from 'react';
import App from '@/app';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);

<ol>
  <li>
    Znajdź i wypełnij formularz PCC-3. Formularze podatkowe są dostępne na
    Portalu Podatkowym (podatki.gov.pl) w usłudze e-Deklaracje. Można je
    podpisać:
    <ul>
      <li>kwalifikowanym podpisem elektronicznym</li>
      <li>danymi autoryzującymi lub</li>
    </ul>
  </li>
</ol>;
